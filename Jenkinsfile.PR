pipeline {

    stages {

        stage('Build') {
            agent { label 'jenkins' }
            steps {
                sh 'echo TEST'
		currentBuild.result = 'UNSTABLE'
            }
        }
    }
}
