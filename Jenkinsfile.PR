pipeline {

    agent none

    stages {

        stage('Build') {
            agent { label 'jenkins' }
            steps {
                sh 'echo TEST'
		sh 'currentBuild.result = 'UNSTABLE'
            }
        }
    }
}
